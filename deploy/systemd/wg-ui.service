[Unit]
Description=WireGuard UI FastAPI service
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/wg-ui
EnvironmentFile=/opt/wg-ui/.env
ExecStart=/opt/wg-ui/venv/bin/uvicorn main:app --host 127.0.0.1 --port 8000
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
